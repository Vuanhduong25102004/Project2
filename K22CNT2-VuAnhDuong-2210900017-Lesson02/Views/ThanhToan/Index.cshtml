@model IEnumerable<K22CNT2_VuAnhDuong_2210900017.Models.THANH_TOAN>

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PhuongThuc)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Trangthai)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DON_HANG.TenDH)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PhuongThuc)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Trangthai)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DON_HANG.TenDH)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.MaTT }) |
                @Html.ActionLink("Details", "Details", new { id=item.MaTT }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.MaTT })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
